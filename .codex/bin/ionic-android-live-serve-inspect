#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CODEX_HOME="${CODEX_HOME:-$(cd -- "$SCRIPT_DIR/.." && pwd)}"

export CODEX_HOME
exec "$CODEX_HOME/bin/run-skill" ionic-android-live-serve-inspect "$@"

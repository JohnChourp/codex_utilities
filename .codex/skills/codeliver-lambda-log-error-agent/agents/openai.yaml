interface:
  display_name: "CodeDeliver Lambda Log Error Agent"
  short_description: "Triage CodeDeliver Lambda errors and auth-drift risk (single + batch + cross-product audit)"
  default_prompt: "Investigate CodeDeliver Lambda failures as an agent-native workflow, with support for single incident triage, batch triage across all local repos in /home/dm-soft-1/Downloads/lambdas/codeliver_all, and cross-product auth-drift audit for codeliver-pos/panel/sap/app. Run deterministic preflight (AWS auth, local repo roots, runtime/arch when function is known), collect log evidence by request id when applicable, correlate across lambdas and /home/dm-soft-1/Downloads/projects/codeliver, separate production failures from smoke/test noise, and produce concrete remediation plus verification steps."

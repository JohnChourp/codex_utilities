interface:
  display_name: "Lambdas Sync codeliver_all"
  short_description: "Bulk fetch/pull + empty repo reclone for codeliver_all"
  default_prompt: "Sync all repositories inside codeliver_all with fetch/pull, auto-reclone empty repos, and generate txt reports with failures and skipped pulls."


interface:
  display_name: "CRP Lambda Log Error Agent"
  short_description: "Triage CRP Lambda errors (single incident + batch crp_all)"
  default_prompt: "Investigate Lambda failures with CRP CLI as an agent-native workflow (no lambda-log-error-url wrapper, no lambda-ai-tools). Support both single incident triage and batch triage across all local repos in /home/dm-soft-1/Downloads/lambdas/crp_all. Run deterministic preflight gates (CRP auth, AWS auth, GitHub org, local crp_all presence, runtime/arch when function is known), gather log/code evidence, enrich feature/master/brain context, separate production failures from smoke/test noise, and return concrete fix plus verification steps."

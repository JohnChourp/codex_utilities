interface:
  display_name: "CRP npm install Repos"
  short_description: "Install dependencies in local CRP lambdas"
  default_prompt: "Run npm i in every direct child repo under /Users/john/Downloads/lambdas/crp_all that includes package.json, using default CONCURRENCY=20 unless overridden via env."
